#inventory location - /etc/ansible/hosts
#version
ansible --version

#ping
ansible -m ping laall

#running ad-hoc command
ansible -a "df -h" laall

#ask/prompt for sudo (become password)
ansible-playbook install-docker.yaml -K
